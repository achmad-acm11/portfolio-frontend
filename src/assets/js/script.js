const projectDetail = document.querySelectorAll(".to-project-detail");

projectDetail.forEach((project) => {
  project.addEventListener("click", function () {
    // location.href = "portfolio-detail.html";
  });
});
